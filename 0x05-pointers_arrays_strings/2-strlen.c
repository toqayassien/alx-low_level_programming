#include "main.h"
/**
 *_strlen -main
 *@s : string
 *return:0
 */
int _strlen(char *s)/*_strlen : for betty*/
{
	int len;
	char ch[] = "StringLength";

	*s = ch;

	len =  _strlen("*s");
	return (0);
}
