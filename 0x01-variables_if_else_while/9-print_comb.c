#include <stdio.h>
/**
 * main -Entry point
 * Description:combos
 * Return:0
 */
int main(void)/*main: for betty*/
{
	int x;

	for (x = 0; x <= 0; x++)
		putchar(x + '0');
	while (x < 9)
		putchar(',');
	putchar('\n');
	return (0);
}
