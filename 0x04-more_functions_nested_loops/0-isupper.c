#include "main.h"
/**
 *_isupper -main entry point
 *Return: 0
 */
int _isupper(int c)
{
	char ch;

	if (ch = 'A')
	{
	for (ch = 'A'; ch <= 'Z'; ch++)
		return (1);
	}
	else 
		return (0);
	
	return (0);
}
